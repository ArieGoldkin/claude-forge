---
description: LangGraph parallel execution patterns. Use when implementing fan-out/fan-in workflows, map-reduce over tasks, or running independent agents concurrently. Triggers on LangGraph parallel, fan-out, fan-in, map-reduce, concurrent agents, parallel branches, Send API
---

# langgraph-parallel

Invoking skill: atk:langgraph

Follow the instructions in the langgraph skill (parallel execution) exactly.

$ARGUMENTS
