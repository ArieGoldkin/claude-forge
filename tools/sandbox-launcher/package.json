{
  "name": "sandbox-launcher",
  "version": "0.1.0",
  "description": "Maintainer-only sandbox launcher for sandboxed ADW dispatch (ADR-0001, issue #45). NOT part of any shipped plugin.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "reap": "tsx src/reap.ts"
  },
  "dependencies": {
    "@vercel/sandbox": "^2.9.0"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^2.1.0"
  },
  "license": "UNLICENSED"
}
